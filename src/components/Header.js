import React from "react";

function Header() {
  return (
    <header>
      <div className="header-quote">
        Its never too early for a <span>Cocktail</span>
      </div>
    </header>
  );
}

export default Header;
